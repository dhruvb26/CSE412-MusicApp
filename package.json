{
  "name": "cse412-music-app",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "postinstall": "uv sync --project apps/api",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "format": "turbo run format",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "turbo": "^2.8.20",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.2",
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "@biomejs/biome": "^2.3.14",
    "lefthook": "^2.1.0",
    "zod": "^4.3.6"
  }
}
